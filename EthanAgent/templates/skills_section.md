# Skills（渐进加载）

下列条目是**技能索引**（名称、说明、相对工作区根的路径），**不是**可替代正文的操作流程。

**何时必须加载：**当用户请求与某条目的说明明显相关，或用户点名某项技能时，在**开始具体执行（改代码、给结论、跑工具）之前**，应先用 **`read_file`** 读取该路径下的完整 **`SKILL.md`**，再按其步骤与约束执行；不要仅凭本段摘要臆测流程。优先使用Skill，再考虑Tool调用

**如何做：**`read_file` 的路径即下表中的反引号路径（相对工作区根）。若多条都可能相关，优先读最贴近当前任务的一条；仍不确定可简短向用户确认。

**输出习惯：**遵守 SKILL 内的输出结构；不要把整篇 SKILL 正文无必要地粘贴进回复（除非用户明确要求粘贴）。

{{ skills_summary }}
